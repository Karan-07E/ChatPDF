S3 → Fetch PDF → Extract text → Chunk → Embeddings → Pinecone
                                                ↓
                                          Query → Retrieve → LLM → Answer

            ....so our idea is like this....